-- Essentially just default config separated into multiple files
-- I don't like reading through a monolith when I'm trying to change one specific thing

-- programs is not required, as it's only used in keybindings
require ("config.appearance")
require ("config.autostart")
require ("config.env-variables")
require ("config.input")
require ("config.keybindings")
require ("config.misc")
require ("config.monitors")
require ("config.permissions")
require ("config.rules")
require ("config.plugins")
